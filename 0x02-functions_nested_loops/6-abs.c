#include <main.h>
/**
 * main -  a function that computes the absolute value of an integer
 * Return: 0 (successful)
 */
int _abs(int)
{
	if (i >= 0)
	{
		return(i);
	}
	else
	{
		return (i * -1);
	}
}
