#include <stdio.h>
/**
 * main -  a program that prints the alphabet in lowercase, followed by a new line
 * return: 0 (successful)
 */
int main(void)
{
	char c;
	c = 'a';
	while
	(c <= 'a')
	{
	putchar(c);
	c++;
	}
	putchar('\n');
	return (0);
}
